# crow-plugins-mirror

Mirrors selected images from `codefloe.com/crow-plugins` to `quay.io/amrkmn/crow-plugins`.

## Automation

GitHub Actions runs `@crow-plugins-mirror.sh` every 2 hours and also supports manual runs from the Actions tab.
